题目：
给一组员工上班/下班时间，返回每个时间点正在上班的员工的数量

思路：
对所有的 输入数据做处理， 例如 x -> y 标记成 在时间点x要加1, 在时间y要减一。
先对所有 员工时间，上班的下班的一起排序，然后见一个Map存结果 key 是时间点， value是 数量，
然后有一个总得 count ＝ 0；对一个数字，判断是上班的话就 ++， 是下班的话就--， 然后把这个值存到Map中。

最后查询的时候, 如果不在map里，遍历这个map（或者用Sortedmap），找到在这个时间点上一个的时间点，它的值就是结果。
